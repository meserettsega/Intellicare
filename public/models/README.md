Place your realistic GLB model here with the exact filename `elephant.glb`.

Path (relative to project root):

  public/models/elephant.glb

Notes:
- Vite serves files in `public/` at the server root. The loader expects `/models/elephant.glb`.
- If you don't provide the GLB, the app falls back to a procedural particle silhouette for local development.
- Ensure the file is a binary GLB (not a .gltf JSON) and that it downloads correctly when opening http://localhost:5173/models/elephant.glb
